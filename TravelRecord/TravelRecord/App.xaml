<?xml version="1.0" encoding="utf-8" ?>
<Application xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="TravelRecord.App">
    <Application.Resources>     <!-- ready made Application.Resources element-->
        <!-- <ContentPage.Resources>-->
            <ResourceDictionary>
                <Color x:Key="eveningishColour">#22DD22</Color>   <!-- NB good not to give styles colour names, in case colour tweaked later -->
                <Color x:Key="wheatleColour">#BBFECC</Color>
            <!-- change once, here, to affect all instances-->
            <Style TargetType="Button">
                <Setter Property="BackgroundColor" Value="{StaticResource eveningishColour}"/>
                <Setter Property="TextColor" Value="{StaticResource wheatleColour}"/> 
            </Style>
            </ResourceDictionary>
        <!-- </ContentPage.Resources> -->
    </Application.Resources>
</Application>